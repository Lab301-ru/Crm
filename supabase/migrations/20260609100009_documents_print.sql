-- =============================================================
-- ЭТАП 11. Печатные документы: файл в Storage опционален
-- =============================================================
-- Документ полностью воспроизводится из snapshot (печатная форма
-- рендерится в браузере, «Сохранить как PDF» делает сам браузер).
-- PDF в Storage не хранится — нет расхода Free tier и рассинхрона
-- файла со снимком. storage_path остаётся для Phase 2 (отправка
-- PDF клиенту по email), поэтому колонка не удаляется.
alter table public.order_documents alter column storage_path drop not null;
